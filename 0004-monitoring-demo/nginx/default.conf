server {
    listen 80;
    server_name localhost;

    location / {
        return 200 "Monitoring Demo - Nginx is running\n";
    }

    location /status {
        stub_status;
        access_log off;
    }
}